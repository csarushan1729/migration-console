# Optional: enables model-assisted mapping for leftover/ambiguous columns.
XAI_API_KEY=

# Optional: where job state is persisted (SQLite, via Node's built-in
# node:sqlite). Defaults to ./data/meridian.db, created on first write.
MERIDIAN_DB_PATH=
