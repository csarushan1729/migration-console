services:
  - type: web
    name: Migration-Console
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: XAI_API_KEY
        sync: false
